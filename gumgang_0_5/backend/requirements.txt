# ===========================
# 금강 2.0 백엔드 의존성
# ===========================
# Python 3.7+ 필요

# -----------------------------
# 핵심 웹 프레임워크
# -----------------------------
fastapi==0.104.1
uvicorn[standard]==0.24.0
gunicorn==21.2.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-multipart==0.0.6

# -----------------------------
# AI/ML 라이브러리
# -----------------------------
numpy==1.24.3
scipy==1.11.4
scikit-learn==1.3.2
pandas==2.1.3

# -----------------------------
# 비동기 처리
# -----------------------------
aiofiles==23.2.1
aiohttp==3.9.1
asyncio-mqtt==0.16.2  # MQTT 지원 (옵션)

# -----------------------------
# 데이터베이스 & 캐싱
# -----------------------------
redis==5.0.1
hiredis==2.2.3  # Redis 성능 향상
motor==3.3.2  # MongoDB 비동기 드라이버
pymongo==4.6.0
sqlalchemy==2.0.23
asyncpg==0.29.0  # PostgreSQL 비동기 드라이버

# -----------------------------
# 보안 & 인증
# -----------------------------
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0
cryptography==41.0.7

# -----------------------------
# HTTP 클라이언트 & WebSocket
# -----------------------------
httpx==0.25.2
websockets==12.0
python-socketio==5.10.0

# -----------------------------
# 유틸리티
# -----------------------------
python-dateutil==2.8.2
pytz==2023.3
pyyaml==6.0.1
orjson==3.9.10  # 빠른 JSON 처리
ujson==5.8.0  # 대안 JSON 라이브러리

# -----------------------------
# 로깅 & 모니터링
# -----------------------------
loguru==0.7.2
sentry-sdk==1.39.1  # 에러 트래킹 (옵션)
prometheus-client==0.19.0  # 메트릭 수집 (옵션)

# -----------------------------
# 개발 도구
# -----------------------------
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-mock==3.12.0
black==23.11.0
isort==5.12.0
flake8==6.1.0
mypy==1.7.1
pre-commit==3.5.0

# -----------------------------
# 문서화
# -----------------------------
mkdocs==1.5.3
mkdocs-material==9.4.14
mkdocstrings[python]==0.24.0

# -----------------------------
# 추가 AI 관련 (옵션)
# -----------------------------
transformers==4.35.2  # Hugging Face Transformers
torch==2.1.1  # PyTorch (GPU 지원 필요시)
# tensorflow==2.15.0  # TensorFlow (대안)
sentence-transformers==2.2.2  # 문장 임베딩
langchain==0.0.340  # LangChain (옵션)
openai==1.3.7  # OpenAI API (옵션)

# -----------------------------
# 데이터 처리
# -----------------------------
pillow==10.1.0  # 이미지 처리
opencv-python==4.8.1.78  # 컴퓨터 비전 (옵션)
matplotlib==3.8.2  # 시각화
seaborn==0.13.0  # 통계 시각화

# -----------------------------
# 성능 최적화
# -----------------------------
cython==3.0.6  # C 확장 (옵션)
numba==0.58.1  # JIT 컴파일 (옵션)
ray==2.8.1  # 분산 처리 (옵션)

# -----------------------------
# 타입 체킹 & 검증
# -----------------------------
pydantic[email]==2.5.0
email-validator==2.1.0
phonenumbers==8.13.26

# -----------------------------
# 작업 큐 & 스케줄링
# -----------------------------
celery==5.3.4  # 비동기 작업 큐
celery[redis]==5.3.4
apscheduler==3.10.4  # 작업 스케줄링
rq==1.15.1  # 간단한 작업 큐 (대안)

# -----------------------------
# 파일 처리
# -----------------------------
python-magic==0.4.27  # 파일 타입 감지
chardet==5.2.0  # 문자 인코딩 감지
openpyxl==3.1.2  # Excel 파일 처리
PyPDF2==3.0.1  # PDF 처리

# -----------------------------
# 환경 설정
# -----------------------------
pydantic-settings==2.1.0
python-decouple==3.8
configparser==6.0.0

# -----------------------------
# 네트워킹
# -----------------------------
requests==2.31.0  # 동기 HTTP 클라이언트
urllib3==2.1.0
certifi==2023.11.17

# -----------------------------
# 텍스트 처리
# -----------------------------
beautifulsoup4==4.12.2  # HTML 파싱
lxml==4.9.3  # XML 처리
markdown==3.5.1  # Markdown 처리
python-markdown-math==0.8

# -----------------------------
# 국제화 (i18n)
# -----------------------------
babel==2.13.1
pytz==2023.3

# ===========================
# 설치 방법:
# pip install -r requirements.txt
#
# 개발 환경만:
# pip install -r requirements-dev.txt
#
# 프로덕션 최소 설치:
# pip install fastapi uvicorn numpy scipy scikit-learn redis
# ===========================
