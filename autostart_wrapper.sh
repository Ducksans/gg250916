#!/bin/bash
# Deprecated autostart wrapper. Left for reference only.

echo "[DEPRECATED] autostart_wrapper.sh 는 더 이상 사용하지 않습니다." >&2
echo "            로그인 후 터미널에서 ./start_servers.sh 를 실행하세요." >&2
exit 1
